query getPoolById ($id:ID) {
  crps (where: {id:$id}) {
    id
    creator {
      id
    }
    token
    firstTokenBalance
    firstTokenWeight
    secondTokenBalance
    secondTokenWeight
    swapFee
  }
}
